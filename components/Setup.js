import{CONFIG}from'../config.js';
export const meta={title:'Setup técnico',subtitle:'Estado de conexión, endpoints, arquitectura y publicación.'};
export function render(s){return`<article class="card"><div class="detailRow"><span>Fuentes de datos</span><p>SEMrush · Google Search Console · Google Analytics 4 · Microsoft Clarity · Google Tag Manager · Google Sheets</p></div><div class="detailRow"><span>DATA_URL</span><p><code>${CONFIG.DATA_URL}</code></p></div><div class="detailRow"><span>POST_URL</span><p><code>${CONFIG.POST_URL}</code></p></div><div class="detailRow"><span>SHEET_URL</span><p><code>${CONFIG.SHEET_URL}</code></p></div><div class="detailRow"><span>Datos cargados</span><p>${s.data.master.length} URLs master · ${s.data.redirects.length} redirects · ${s.data.protected.length} protegidas · ${s.data.checklist.length} tareas</p></div><div class="detailRow"><span>Arquitectura V7</span><p>Componentes JS por menú, gráficos por vista, edición desde frontend y actualización inmediata del estado local tras guardar.</p></div></article>`}
