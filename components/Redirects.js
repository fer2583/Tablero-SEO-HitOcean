import{kpis}from'./common.js';import{bar,countBy,esc,fmt,num,path,pill,pclass,donuts}from'../services/utils.js';
export const meta={title:'Redirects',subtitle:'Control para evitar pérdida de tráfico: origen, destino, status, prioridad y recomendación.'};
export function render(s){const v=s.data.redirects.filter(r=>s.filtered.some(x=>x.url===r.url));const missing=v.filter(r=>!r.target).length,high=v.filter(r=>/alta|high/i.test(r.priority)).length,ok301=v.filter(r=>num(r.status)===301).length;return`${kpis(s.filtered,s)}<div class="grid2"><article class="card"><div class="chartTitle">Salud de redirects</div>${donuts([{label:'Destino pendiente',value:missing,total:v.length},{label:'Alta prioridad',value:high,total:v.length},{label:'Status 301',value:ok301,total:v.length}])}</article><article class="card"><div class="chartTitle">Redirects por acción</div>${bar(countBy(v,'action'))}</article></div><div class="grid4"><article class="metric"><span>Redirects</span><strong>${fmt.format(v.length)}</strong></article><article class="metric"><span>Destino pendiente</span><strong>${fmt.format(missing)}</strong></article><article class="metric"><span>Alta prioridad</span><strong>${fmt.format(high)}</strong></article><article class="metric"><span>Status 301</span><strong>${fmt.format(ok301)}</strong></article></div><div class="grid2"><article class="card"><div class="chartTitle">Prioridad</div>${bar(countBy(v,'priority'))}</article><article class="card"><div class="chartTitle">Status HTTP</div>${bar(countBy(v,'status'))}</article></div><article class="card tableCard" style="margin-top:16px"><div class="tableTop"><span>${fmt.format(v.length)} redirects</span><span>Revisar destinos pendientes</span></div><div class="tableWrap"><table><thead><tr><th>Origen</th><th>Prioridad</th><th>Acción</th><th>Status</th><th>Destino</th><th>Issues</th><th>Recomendación</th></tr></thead><tbody>${v.slice(0,800).map(r=>`<tr data-select-url="${encodeURIComponent(r.url)}"><td><div class="url">${esc(path(r.url))}</div></td><td>${pill(r.priority,pclass(r.priority))}</td><td>${pill(r.action,'blue')}</td><td>${pill(r.status||'—',num(r.status)===301?'teal':num(r.status)===200?'baja':'gray')}</td><td>${r.target?`<a class="url" href="${esc(r.target)}" target="_blank">${esc(r.target)}</a>`:pill('Destino pendiente','media')}</td><td>${esc(r.issues||'—')}</td><td>${esc(r.recommendation||'—')}</td></tr>`).join('')}</tbody></table></div></article>`}
